server:
  port: 8063

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wallet_auth_db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

