server:
  port: 8064

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wallet_user_db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

